<Window x:Class="SmartPS.Views.Common.NotificationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartPS.Views.Common"
        mc:Ignorable="d"
        Title="Thông báo hệ thống"
        Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource CardBackgroundBrush}"
        FocusManager.FocusedElement="{Binding ElementName=PrimaryButton}"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <!-- Nút Phụ (Hủy bỏ / Không) -->
        <Style x:Key="DialogSecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinWidth" Value="85"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F8FAFC"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#94A3B8"/>
                                <Setter Property="Foreground" Value="#0F172A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1F5F9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Nút Chính (Đồng ý / OK) -->
        <Style x:Key="DialogPrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinWidth" Value="95"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>    <!-- Body Content -->
            <RowDefinition Height="Auto"/> <!-- Footer Actions -->
        </Grid.RowDefinitions>

        <!-- ROW 0: BODY (Icon hộp trực quan + Tiêu đề + Nội dung) -->
        <Grid Grid.Row="0" Margin="20,18,20,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Khung Hộp Icon (38x38) -->
            <Border Grid.Column="0"
                    x:Name="IconBadgeBorder"
                    Width="38" Height="38"
                    CornerRadius="4"
                    BorderThickness="1"
                    VerticalAlignment="Top"
                    Margin="0,2,14,0">
                <Path x:Name="IconPath"
                      Width="18" Height="18"
                      Stretch="Uniform"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
            </Border>

            <!-- Nội dung văn bản -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <!-- Tiêu đề thông báo -->
                <TextBlock x:Name="TitleTextBlock"
                           Text="Tiêu đề thông báo"
                           FontSize="14"
                           FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,4"/>

                <!-- Nội dung chi tiết -->
                <TextBlock x:Name="MessageTextBlock"
                           Text="Nội dung thông báo hiển thị tại đây."
                           FontSize="12.5"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           TextWrapping="Wrap"
                           LineHeight="19"/>
            </StackPanel>
        </Grid>

        <!-- ROW 1: FOOTER (Thanh nút bấm) -->
        <Border Grid.Row="1"
                Background="#F8FAFC"
                BorderBrush="{StaticResource DividerBrush}"
                BorderThickness="0,1,0,0"
                Padding="16,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <!-- Nút Phụ (Hủy bỏ / Không) -->
                <Button x:Name="SecondaryButton"
                        Style="{StaticResource DialogSecondaryButtonStyle}"
                        Content="Hủy bỏ"
                        Margin="4,0"
                        Visibility="Collapsed"
                        Click="SecondaryButton_Click"/>

                <!-- Nút Chính (Đồng ý / OK) -->
                <Button x:Name="PrimaryButton"
                        Style="{StaticResource DialogPrimaryButtonStyle}"
                        Content="Đồng ý"
                        Margin="4,0"
                        Click="PrimaryButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
